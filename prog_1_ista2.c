#include <stdio.h>

void main() {
        int numero;
        printf("informe um numero inteiro: ");
        scanf("%d",&numero);
        if(numero % 2 == 0) {
           printf("seu numero %d e par",numero);
                            }else 
                            {printf("numero %d impar",numero);};
            }
